# GhostHound-
An OSINT Tool
# Clone the repository from GitHub
git clone https://github.com/kuzenispupan106-afk/GhostHound-.git

##Move into the project directory
cd GhostHound-

#Install all the required framework dependencies
pip install python-dotenv rich requests colorama phonenumbers python-whois dnspython beautifulsoup4 aiohttp validators --break-system-packages

#Make the tool executable
chmod +x GhostHound

#Execute the tool
./GhostHound
